#!/bin/bash

case "$*" in
    "action list-panes --json -a")
        command cat "$ZELLIJ_TEST_PANES_FILE"
        ;;
    "action list-tabs --json")
        command cat "$ZELLIJ_TEST_TABS_FILE"
        ;;
    *)
        echo "Unexpected zellij arguments: $*" >&2
        exit 1
        ;;
esac
